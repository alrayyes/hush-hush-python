# Markdown this repo owns. The README is the first thing an integrator reads
# off this SDK, and a broken link or a skipped heading level is exactly the
# sort of thing nobody notices while writing it.
globs:
  - "**/*.md"

ignores:
  - CHANGELOG.md # written by the release job, not by hand
  - "**/node_modules/**"
  - "**/.bun-cache/**"
  - "**/.venv/**"
  - "site/**" # mkdocs build output
  - "hush-hush/**" # the pinned submodule — someone else's docs, someone else's rules
  # A historical planning record in OpenSpec's own format, not this repo's
  # prose. openspec/README.md is ours and stays covered.
  - "openspec/proposal.md"
  - "openspec/design.md"
  - "openspec/tasks.md"
  - "openspec/specs/**"
  # Vale's own style packages, fetched by `vale sync`, not committed - never
  # ignore styles/ wholesale, or styles/House (this repo's own vocabulary)
  # goes with it.
  - "styles/Google/**"
  - "styles/proselint/**"

config:
  line-length:
    line_length: 80
    tables: false
    code_blocks: false
  # Python code blocks are formatted by ruff, which uses real tabs nowhere —
  # this stays default (checked) unlike the Go repos' override.

extends: "markdownlint/style/prettier"
