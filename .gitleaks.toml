# Secret scanning for hush-hush-python.
#
# Extends the ruleset that ships with the pinned gitleaks version rather than
# replacing it, so new upstream rules arrive with the version bump.
#
# Nothing is allowlisted yet. Add an entry here rather than reaching for an
# ignore comment if this ever needs one.

[extend]
useDefault = true
