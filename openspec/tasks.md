## 1. Repo setup

- [x] 1.1 Create `github.com/alrayyes/hush-hush-python`, public, from a suitable scaffold (repo-creation skill), and verify CI runs on the initial empty commit
- [x] 1.2 Add hush-hush's `api/` as a git submodule pinned to its current commit, and verify `git submodule status` shows a clean pin
- [x] 1.3 Set up `pyproject.toml` via `uv` targeting Python 3.11+, and verify `uv sync` succeeds on a bare project

## 2. Codegen pipeline

- [x] 2.1 Configure `openapi-python-client` to generate types and a client from the submodule's `openapi.yaml`, and verify generated files carry the `# Code generated by openapi-python-client; DO NOT EDIT.` header — added by `scripts/generate.sh` since the tool doesn't stamp one itself
- [x] 2.2 Wire generation into a `uv run` script, and verify running it twice against the same submodule commit produces a byte-identical generated tree
- [x] 2.3 Add a CI check that fails if the generated package differs from what regenerating from the pinned submodule commit produces, and verify it fails on a manual edit to a generated file

## 3. Hand-written client layer

- [x] 3.1 Implement `Client` and `AsyncClient` constructors (base URL, credential with `HUSH_HUSH_API_KEY` env fallback, timeout, max retries) on top of `httpx`, and verify both scenarios in specs/sdk-python/spec.md's "Client construction" requirement pass as unit tests
- [x] 3.2 Implement the `X-Caller` per-call keyword argument and verify it's attached per-request, not client-wide
- [x] 3.3 Implement retry/backoff (network failures, 5xx, 429 only; honors `Retry-After`) shared by both clients, and verify the three scenarios in the "Retry behavior" requirement pass for both sync and async
- [x] 3.4 Implement the typed exception hierarchy (status, parsed body, optional request ID) and verify the "Typed error mapping" requirement's scenario passes
- [x] 3.5 Implement the typed audit-log query method (filters, flat list result — no iterator, hush-hush's `/audit-log` has no pagination) on both clients, and the `get_object_used_by` method, and verify the "Audit log query" and "Typed resource operations" requirements' scenarios pass

## 4. Testing

- [x] 4.1 Add unit tests for the hand-written layer only (3.1-3.5) against a fake transport, and verify `pytest` passes with no network calls — used a real local HTTP server (Python's http.server), not a mock, matching the Go SDK's httptest approach
- [x] 4.2 Stand up a Prism mock server from the pinned spec in CI and run the full client surface (sync and async) against it, and verify contract tests pass
- [x] 4.3 Add a Pact consumer test recording the client's real interactions, and verify a pact file is produced and published as a CI artifact — pact-python 3.x bundles its native FFI as a normal dependency, no separate install step needed (unlike Go)
- [x] 4.4 Add a thin end-to-end smoke test against a staging hush-hush instance, gated to run on release/nightly only, and verify it's excluded from the default PR pipeline

## 5. CI and release

- [x] 5.1 Add a scheduled/webhook-triggered regeneration workflow that opens a PR with the result, and verify it never pushes directly to the default branch
- [x] 5.2 Add an `openapi-diff` step classifying each regeneration PR's underlying spec change as additive or breaking, and verify the classification is visible on the PR even though it doesn't gate merge
- [x] 5.3 Configure full auto-merge for this repo's PRs (scaffolding and every regeneration PR) on green CI, per the session's auto-merge grant, and verify a green-CI PR merges without manual approval — branch protection is set on this repo (unlike go-sdk's first attempt), so auto-merge genuinely waits for CI here
- [x] 5.4 Wire semver bump decisions to the `openapi-diff` classification rather than Conventional Commits, and verify a test release computes the expected bump for both an additive and a breaking sample diff

## 6. Docs

- [x] 6.1 Add docstrings to all public classes/methods in the hand-written layer, and verify a docstring-coverage check reports no gaps
- [x] 6.2 Build an mkdocs site from those docstrings, and verify it builds without warnings — verified locally with `--strict`, deployed to GitHub Pages via CI
- [x] 6.3 Write the README quickstart (install, auth setup, one real call) by hand, and verify a fresh reader can follow it to a successful first call against a local Prism mock
- [x] 6.4 Add README status badges (CI, PyPI, license), and copy this change's proposal/design/spec/tasks into an `openspec/` directory in the repo itself so it stands on its own without depending on hush-hush-sdk being reachable

## 7. Cross-repo wiring

- [x] 7.1 Once the repo has real content and passes CI, request hush-hush's README be updated to link to it (coordinate with whoever owns that repo), and verify the link is live — alrayyes/hush-hush#84, merged, link confirmed live in the README's SDKs section
- [x] 7.2 Close hush-hush issue #77, linking to the new repo, and verify the issue is closed with that link in its final comment
- [x] 7.3 Note in hush-hush issue #76 that the Python package is ready for manual PyPI publish
- [x] 7.4 Wire up PyPI Trusted Publishing in the release job once a PyPI account and pending publisher exist — hush-hush-python#21
